# AGENTS.md — SynaptixFit

## Entorno de Desarrollo
- **SO:** WSL Ubuntu (Linux)
- **Editor:** LazyVim (Neovim)
- **Shell:** zsh

## Tech Stack
- **Frontend:** Flutter (Dart 3.3+) at `app/` — Riverpod, GoRouter, Supabase SDK, Hive, fl_chart, connectivity_plus, file_picker, flutter_markdown/markdown
- **Backend:** Supabase (PostgreSQL + Auth + Realtime) — no traditional server, no Node.js/Express
- **Storage proxy:** Cloudflare Worker at `cloudflare/synaptixfit-r2-proxy/worker.js` + config `wrangler.jsonc` (binding `R2_BUCKET`)
- **No `package.json`, no `yarn`, no npm`** — this is a Flutter repo, not Node.js
- **No CI/CD** implemented yet

## Language convention
- Code (variables, files, endpoints): **English**
- Comments (DartDoc) and docs/ files: **Spanish**

## Essential commands

### Flutter (run inside `app/`)
```bash
flutter pub get                  # install deps
dart format .                    # format all Dart code
flutter analyze                  # lint (uses flutter_lints from analysis_options.yaml)
flutter test                     # unit + widget tests
flutter test --coverage          # with coverage
dart run build_runner build      # regenerate Riverpod .g.dart files after changing providers
flutter run -d chrome            # web dev
flutter build apk --release      # Android release
```

### Supabase (run from repo root)
```bash
supabase db push                 # deploy migrations to linked project
supabase functions deploy --all  # deploy edge functions (if any)
```

### Data seeding (Python, from repo root)
```bash
python supabase/seed_catalogo_v2.py    # populate academic catalog v2 from grados.json
```

### DB Sync (local ↔ remote)
```bash
# Poblar remoto desde local
python supabase/seed_catalogo_v2.py

# Volcar remoto a migración local
supabase db dump --linked --data-only > supabase/seed_data.sql

# Aplicar migraciones pendientes a local y remoto
python supabase/apply_migrations.py
```

### DB migrations (manual fallback)
```bash
supabase db push                 # deploy migrations to linked project
# If CLI not linked, run migraciones_pendientes.sql in Supabase SQL Editor
```

## Architecture at a glance
- **`app/lib/main.dart`** — entry point, loads `.env`, initializes Supabase, wraps in ProviderScope
- **`app/lib/core/routing/app_router.dart`** — GoRouter with shell route + bottom nav (5 tabs)
- **`app/lib/shared/models/db_models.dart`** — all DB model classes (~2935 lines, incl. `MaterialEstudioDb` con SM-2, `BancoPreguntasDb`, `PreguntaDb`, `IntentoPreguntaDb`, `CargaAcademicaSemanalDb`, `AsignaturaUsuarioSemestreDb`, `EjercicioDb` con `valorMet`, `SeleccionEjercicioDb` con `duracionObjetivoSegundos`/`duracionRealSegundos`, `EstadoCognitivoUsuarioDb`, `EstadoRegulacionCruzadaDb`, `RegistroRepasoSrsDb`, `RegistroCargaFisicaDb`)
- **`app/lib/shared/widgets/metric_gauge.dart`** — animated radial gauge CustomPainter (1200ms) for dashboard metrics
- **`app/lib/core/config/env_config.dart`** — reads Supabase URL/anonKey, Google OAuth, Gemini, R2 from `.env`
- **`app/lib/features/bienestar/infrastructure/recomendacion_ia_service.dart`** — Gemini prompts with JSON mode + smart catalog (top 60) + unified context formatting (~1357 lines). `refinarRutina` pide nombres de semanas/días, prohíbe incluir duraciones en el nombre y `_sanitizarNombreRutina()` corrige menciones erróneas («4 semanas» en rutinas de 1); `_parseError()` mapea 429 a mensaje corto de cuota y lee cuerpos de error no-JSON
- **`app/lib/features/bienestar/infrastructure/calorie_calculator_service.dart`** — Servicio de cálculo calórico MET (Compendio Adultos 2024): fórmula `(valorMet × pesoUsuarioKg × duracionSegundos / 3600)` con fallback 70 kg, 5 métodos estáticos (~116 lines)
- **`app/lib/features/bienestar/infrastructure/recomendacion_orquestador_service.dart`** — pipeline orchestrator: 7 stages + fallback + param preservation (~412 lines). `ResultadoGeneracion` propaga `nombresSemanas`/`nombresDias` de la IA (para mostrarlos en creación y persistirlos); nombre base con plural correcto («1 semana»)
- **`app/lib/features/bienestar/infrastructure/recomendacion_contexto_service.dart`** — ContextoAcademico DTO + FCT calculation + energy gates (~381 lines)
- **`app/lib/features/bienestar/infrastructure/recomendacion_reglas_service.dart`** — deterministic rule engine with per-modality parameters (~710 lines)
- **`app/lib/features/bienestar/infrastructure/progresion_calculator.dart`** — progressive overload with isometric support (~380 lines)
- **`app/lib/features/bienestar/infrastructure/study_calorie_service.dart`** — ★ Fórmula 1 (Neurofisiología v8.0): Mifflin-St Jeor RMR + gasto calórico de bloques académicos con MET cognitivo (1.3 lectura, 1.8 clase). 3 métodos estáticos (~75 lines)
- **`app/lib/features/bienestar/infrastructure/cognitive_load_calculator_service.dart`** — ★ Fórmula 2 (Neurofisiología v8.0): C_acum(n) exponencial con ρ=0.1/λ=0.05, A(t)=A₀·e^(−β·t) con β=0.02, dificultad por asignatura. 3 métodos estáticos (~80 lines)
- **`app/lib/features/bienestar/infrastructure/cross_regulation_service.dart`** — ★ Fórmula 3 (Neurofisiología v8.0): V_mod asintótico (σ=0.15, λ_s=0.40) + T_max por tramos ACWR (α=0.5). 2 métodos estáticos (~85 lines)
- **`app/lib/features/academico/infrastructure/sm2_physio_service.dart`** — ★ Fórmula 4 (Neurofisiología v8.0): Q_adj = Q_real + η·(carga_hoy/carga_max), η=0.5. Retorna double sin redondeo (~35 lines)
- **`app/lib/features/bienestar/application/neurofisiologia_provider.dart`** — ★ 6 providers neurofisiológicos: `estadoCognitivoProvider`, `estadoRegulacionCruzadaProvider`, `caloriasEstudioHoyProvider` (SUM tiempo real), `cargaFisicaHoyProvider`, `cargaFisicaMaximaProvider`, `tMaxEstudioProvider` (derivado de CrossRegulationService) (~130 lines)
- **`app/lib/features/bienestar/application/rutina_provider.dart`** — all routine + academic + energy providers (~1912 lines, incl. `adherenciaAcademicaProvider`, `estadoEnergeticoProvider`, `contextoAcademicoProvider`, `diaPendienteProvider`, `syncCargaAcademicaSemanal`, `otorgarXp()`, `finalizarSesion()` con `duracionRealPorEjercicio` + llamada RPC `recalcular_regulacion_cruzada`, DTO `XpResultado`, `EjercicioInput` con `duracionObjetivoSegundos`/`duracionRealSegundos`, `_obtenerPesoUsuarioSesion()` desde `perfil_bienestar_usuario`, nuevos métodos `desmarcarSesion()`, `completarBloqueEstudio()`, `desmarcarBloqueEstudio()`)
- **`app/lib/features/perfil/application/perfil_provider.dart`** — profile data cache with selective invalidation (PerfilCambio enum)
- **`app/lib/shared/models/timeline_item.dart`** — DTO `TimelineItem` with enum `TimelineTipo` (9 values) + enum `ClassType` (theory/practice) + 5 factory constructors + convenience getter `classType` (~233 lines). Campos `isPrivate` y `tipoClase` para metadatos de privacidad y tipo de clase.
- **`app/lib/features/dashboard/application/timeline_provider.dart`** — `timelineHoyProvider` with 5 parallel queries + `diaPendienteProvider` unificado (~91 lines)
- **`app/lib/features/analitica/domain/metrica_semanal_dto.dart`** — DTO `MetricaSemanal` con factory `fromMap` para datos de `v_analitica_semanal`
- **`app/lib/features/analitica/domain/insight_correlacion_dto.dart`** — DTO `InsightCorrelacion` para resultados de correlacion Pearson
- **`app/lib/features/analitica/domain/periodo_analitica.dart`** — enum `PeriodoAnalitica` (semanal/mensual/trimestral) con getters `semanas` y `etiqueta`
- **`app/lib/features/analitica/infrastructure/analitica_repository.dart`** — `AnaliticaRepository` consulta `v_analitica_semanal` + `carga_academica_semanal`, calcula correlacion Pearson (~220 lines)
- **`app/lib/features/analitica/infrastructure/insight_generator.dart`** — generador estatico de frases interpretativas en espanol (racha, consistencia, volumen, correlacion)
- **`app/lib/features/analitica/application/analitica_provider.dart`** — 6 providers Riverpod: `analiticaRepositoryProvider`, `analiticaSemanalProvider`, `tendenciaRpeProvider`, `volumenSemanalProvider`, `correlacionCargaProvider`, `periodoSeleccionadoProvider`
- **`app/lib/features/sync/domain/connectivity_state.dart`** — enum `ConnectivityState` (online/offline/syncing)
- **`app/lib/features/sync/infrastructure/connectivity_service.dart`** — Stream de estado de conectividad (connectivity_plus)
- **`app/lib/features/sync/infrastructure/offline_queue_service.dart`** — cola Hive para operaciones pendientes con reintentos (max 3)
- **`app/lib/features/sync/application/sync_provider.dart`** — providers Riverpod: `connectivityStateProvider`, `offlineQueueServiceProvider`, `offlineQueueLengthProvider`, `syncProvider`
- **`app/lib/features/pomodoro/domain/pomodoro_session.dart`** — DTO con estados de sesión Pomodoro (idle/estudio/descanso/pausado)
- **`app/lib/features/pomodoro/application/pomodoro_provider.dart`** — `pomodoroProvider` (StateNotifier con Timer.periodic, 25/5 min)
- **`app/lib/features/pomodoro/presentation/pomodoro_screen.dart`** — Pantalla con anillo CustomPainter y controles (iniciar/pausar/reanudar/reiniciar/skip)
- **`app/lib/features/pomodoro/presentation/widgets/pomodoro_progress_painter.dart`** — Anillo circular animado que decrece con el tiempo restante
- **`app/lib/features/escanear/domain/escanear_result.dart`** — DTO con texto escaneado y metadatos
- **`app/lib/features/escanear/infrastructure/scanner_service.dart`** — Abstracción `ScannerService` con implementaciones condicionales Web/Mobile
- **`app/lib/features/escanear/application/escanear_provider.dart`** — Provider del servicio de escaneo
- **`app/lib/features/escanear/presentation/escanear_screen.dart`** — Pantalla dual: Web (mensaje informativo), Mobile (TextField + guardar como apunte Markdown)
- **`app/lib/features/social/domain/social_dto.dart`** — DTOs `Publicacion` y `Comentario` con factory `fromMap` que resuelven joins de Supabase (usuarios, conteos, mi_like)
- **`app/lib/features/social/infrastructure/social_repository.dart`** — `SocialRepository` con queries reales: feed paginado, likes, comentarios (CRUD con soft delete), notificaciones best-effort
- **`app/lib/features/social/application/social_provider.dart`** — providers Riverpod: `socialFeedProvider`, `socialCommentsProvider.family`, `likeStateProvider.family`; mutaciones: `toggleLike`, `publicarEnFeed`, `enviarComentario`, `editarComentarioMutation`, `eliminarComentarioMutation`
- **`app/lib/features/social/presentation/muro_social_screen.dart`** — `ConsumerStatefulWidget` con filtros temporales, RefreshIndicator, FAB → BottomSheet para crear publicaciones
- **`app/lib/features/social/presentation/widgets/feed_item_card.dart`** — Card de publicación con avatar, tipo, likes animados, sección de comentarios expandible con `ComentarioInput`
- **`app/lib/features/social/presentation/widgets/comentario_card.dart`** — Card individual de comentario con avatar, editar/eliminar para autor
- **`app/lib/features/social/presentation/widgets/comentario_input.dart`** — Barra de input de comentario con validación 1-500 chars
- **`app/lib/features/insignias/domain/insignia_dto.dart`** — DTOs `Insignia` (catálogo con rareza/color) y `RachaState` (estado de racha con hitos)
- **`app/lib/features/insignias/infrastructure/insignias_repository.dart`** — `InsigniasRepository` consulta catálogo LEFT JOIN usuario_insignias, otorga por UNIQUE constraint
- **`app/lib/features/insignias/infrastructure/insignia_engine.dart`** — `InsigniaEngine` evalúa 13 criterios contra BD y otorga insignias automáticamente (~258 lines). Incluye métrica `semanas_plan_adherencia` para insignia "Planificador Maestro" (4 semanas consecutivas con ≥80% adherencia).
- **`app/lib/features/insignias/infrastructure/racha_service.dart`** — `RachaService` calcula racha diaria (sesiones + check-ins), hitos (7/30/100/365), riesgo (<4h)
- **`app/lib/features/insignias/application/insignias_provider.dart`** — 6 providers: `catalogoInsigniasProvider`, `insigniasUsuarioProvider`, `insigniasCountProvider`, `rachaStateProvider`, `insigniasRecienObtenidasProvider`; acción `evaluarInsignias()` + toast `mostrarInsigniaToast()`
- **`app/lib/features/insignias/presentation/insignias_screen.dart`** — `ConsumerStatefulWidget` con filtro por categoría (6 chips), grid 2 columnas, bottom sheet de detalle
- **`app/lib/features/insignias/presentation/widgets/insignia_card.dart`** — Card de insignia (obtenida = color vibrante, bloqueada = gris + candado)
- **`app/lib/features/insignias/presentation/widgets/racha_indicator.dart`** — Widget de racha con barra de progreso, alerta de riesgo, récord histórico
- **`app/lib/features/retos/`** (Flujo de creación unificado v8.2) — `RetosScreen` (vista Mis retos/Comunidad con filtro «Tareas» Todas/Con/Sin), `CrearRetoScreen` (ruta `/retos/crear`, «Nuevo reto» CLEAN UI con tareas opcionales máx. 12 → `hitos_de_reto`; constructor `prefilledSubjectId`/`prefilledSubjectName`/`prefilledTitle`/`asignaturaFija` desde `extra`; secciones Detalles (esfuerzo con etiquetas amigables Tranquilo/Equilibrado/Intenso → mapeo interno baja/media/alta vía `etiquetasEsfuerzo`/`etiquetaEsfuerzo()` en retos_core, sin mostrar XP), Fechas, Visibilidad, Tareas con adjuntos; barra inferior fija; mapeo directo a `retos` + `hitos_de_reto` con `apunte_id`/`archivo_id`), `crear_reto_simple_sheet.dart` (`mostrarCrearRetoSimpleSheet` — sheet express ampliado y REUTILIZABLE: devuelve el `RetoDb` creado, acepta `prefilledTitle`/`prefilledSubjectId`/`prefilledSubjectName` para acoplarse al contexto, enlace «Añadir tareas» que abre la pantalla completa conservando título y asignatura vía `extra: {...}`; modo edición vía `retoId` → `editarRetoSimple()`), `adjuntar_tarea_sheet.dart` (`mostrarAdjuntarTareaSheet` — selector de apuntes/archivos existentes del usuario + subida de archivo nuevo vía `ArchivosAsignaturaRepository` que queda registrado en `archivos_asignatura` vinculado a la asignatura efectiva de la tarea y visible en Académico), `vincular_sheet.dart` (`mostrarVincularSheet` — vinculación del reto a examen/entrega pendiente (filtrada por la asignatura del reto) o a un bloque de estudio futuro del calendario; tipo 'bloque' en `entidad_vinculada_tipo`), `DetalleRetoScreen` (ruta `/retos/:id`, chips «Apunte/Archivo adjunto» en tareas que abren el visor correspondiente). Sin distinción simple/complejo visible. Eliminados `crear_reto_simple_screen.dart` y `crear_reto_complejo_screen.dart` (legacy, sin ruta).
- **`app/lib/features/admin/`** — Panel de administración Fase 3 (final): `AdminHubScreen` (TabBar 5 tabs: KPIs, Usuarios, Contenido, Ejercicios, Auditoría), `AdminPanelScreen` (refactorizado como pestaña Usuarios con búsqueda, filtros, ordenamiento y botón eliminar), `AdminUsuarioDetalle` (3 sub-pestañas: Perfil/Estadísticas/Timeline con gráficos fl_chart y timeline real, configuración de usuario: editar nombre/email, reset XP, cambiar nivel, eliminar usuario), `AdminWipeDialog`; 7 DTOs en domain/ (`UsuarioAdmin`, `AdminMetricasGlobales`, `AuditoriaRegistro`, `ContenidoReportado`, `AdminEjercicio`, `AdminUsuarioEstadisticas`, `AdminTimelineEntry`), 6 repositorios en infrastructure/ (`AdminRepository` extendido con `deleteUser()`, `AdminMetricasRepository`, `AdminAuditoriaRepository`, `AdminContenidoRepository`, `AdminEjercicioRepository`, `AdminUsuarioStatsRepository`), 6 provider files en application/ (`admin_provider.dart` con `eliminarUsuario()` y mutaciones de configuración, `admin_metricas_provider.dart`, `admin_auditoria_provider.dart`, `admin_contenido_provider.dart`, `admin_ejercicio_provider.dart`, `admin_usuario_stats_provider.dart`), 15 widgets/pantallas en presentation/ (3 pantallas + 12 widgets: `AdminKpiCard`, `AdminKpiDashboard` con gráfico de tendencia 30 días fl_chart, `AdminLogEntry`, `AdminAuditoriaList`, `AdminPaginacionBar`, `AdminWipeDialog`, `AdminContenidoCard`, `AdminContenidoList`, `AdminEjercicioCard`, `AdminEjercicioList`, `AdminGraficosUsuario`, `AdminTimelineUsuario`); ruta `/admin` protegida por rol; `errorBuilder` añadido a todos los `Image.network`
- **`app/lib/features/academico/`** (Sistema de Repaso Espaciado SM-2) — `sm2_calculator.dart` (algoritmo SM-2 escala 0-5: parámetro `calidad` como `double`, fórmula EF `(5−q)`, umbral fallo `calidad.round() < 3`, clamp EF [1.3, 3.0]), `sm2_physio_service.dart` (Fórmula 4: Q_adj por fatiga serotoninérgica), `practica_screen.dart` (ruta `/academico/practica/:materialId`, preguntas secuenciales con feedback inmediato, modal de autoevaluación 5 niveles con intervalos pre-calculados por Sm2PhysioService, auditoría en `registros_repaso_srs`, inyección de repaso en timeline vía `horarios_academicos` con `tipo_actividad='repaso'`, `es_fijo=false`), `practica_provider.dart` (`practicaRepositoryProvider`, `bancoPreguntasProvider.family`), `materiales_estudio_provider.dart` (`materialesEstudioRepositoryProvider`, `materialesAsignaturaProvider.family`)
- **`app/lib/features/academico/`** (Time-Blocking — Lienzo Continuo): (Stack+Positioned+Draggable+ DragTarget, 0 dependencias), `InboxScreen` (ruta `/academico/planificar`, sliders de estudio/deporte + barra de energía), `CanvasScreen` (ruta `/academico/planificar/canvas`, grid 7×16h con Drag & Drop + SnackBar XP, fondo oscuro #1A1A2E, barra de navegación semanal infinita `< Anterior | Fechas | Siguiente > | Hoy`, eje horario inline, barra inferior solo "← Volver" y "Guardar plan" — eliminado el botón "Rutina" independiente), `AcademicBlockSheet` (sheet unificado para crear/editar bloques con pestaña Deporte que integra distribución completa de rutina: selector de rutina, switch "Distribuir rutina completa" con selector de días FilterChips L-D, DatePicker de inicio, resumen de bloques, consulta `dias_rutina` reales vía `semana_id`; tipo Examen: la hora indicada pasa a ser la hora de inicio del bloque (como Clase); tipo Reto: sin selector de duración, el botón «Crear reto» abre `mostrarCrearRetoSimpleSheet` y vincula el reto creado al bloque), `RutinaConfigSheet` (conservado pero ya no se usa desde canvas; solo invocado por `placeRutinaDistribuida`), widgets (`TimeGridPainter`, `TimeBlockWidget` con indicador visual de rutina: barra blanca semitransparente + nombre del día + nombre de rutina en subtitle para bloques con `diaRutinaId != null`, `ProgressGamificationBar`, `ConflictBanner`), DTOs (`InboxConfig`, `TimeBlock` con campos `fecha`, `esHitoInamovible` y getter `diaSemanaEfectivo`, `SemanaGenerada`, `CalendarGridState` con campos `semanaOffset`, `fechaInicioPantalla`, `sincronizando`, `cambiosSinGuardar` (flag determinista del diálogo de salida) y getter `fechaFinPantalla` en `domain/calendar_dtos.dart`; `TimeBlockTipo` con 9 valores: estudio, deporte, clase, descanso, comida, sueno, entrega, examen, repaso), providers (`inboxConfigProvider`, `entregasPendientesProvider`, `asignaturasActivasInboxProvider`, `rutinasActivasInboxProvider`, `horariosFijosProvider`, `calendarGridProvider` con debounce 500ms, persistencia instantánea en moveBlock/resizeBlock, guardado robusto en `guardarPlan()` (reutiliza el plan de la sesión, inserta bloques pendientes con `_rowDeBloque()` y persiste fechas en UTC), carga resiliente de bloques guardados con fallback sin joins, restricción de no crear bloques en horas/fechas pasadas (celda, drop y sheet), `placeRutinaDistribuida()` para distribución completa de rutina usando columna correcta `semana_id` en `dias_rutina`, `bloque_estudio_provider.dart` con `toggleBloqueCompletado()`). Eliminados `PlanSemanalScreen` y la ruta `/plan-semanal` (obsoletos desde el lienzo): al pulsar Guardar el usuario PERMANECE en el lienzo., `@Deprecated` en `inyectarRutinaCascada` (queries columnas inexistentes `dia_semana`, `enfoque` en `dias_rutina`), `TimeblockIaService` (Gemini Flash, prompt español, N1-N10 + H1-H5, fallback heurístico), `GridMath` (conversión hora↔píxel con snap 30min + 5 nuevos métodos: `fechaToColumnIndex`, `columnIndexToFecha`, `fechaToOffsetX`, `offsetXToColumnIndex`, `dayHeaderLabel`), XP unificado (plan 100+5×bloques, bloque ceil(mins/30)×10, entrega 30, check-in 20). Eliminados `wizard_plan_provider.dart`, `crear_plan_semanal_screen.dart`, `gestion_asignaturas_screen.dart` y ruta `/plan-semanal/crear` (deprecados en v7.1).
- **`app/lib/features/academico/` (Escaneo de horarios con IA)** — `infrastructure/ai_schedule_parser_service.dart` (`AiScheduleParserService` + `AiParsingException`: Gemini multimodal (`EnvConfig.geminiModel`, por defecto `gemini-3.6-flash`) con `inline_data` base64, prompt determinista que inyecta las asignaturas activas y extrae SOLO el patrón semanal, saneamiento de Markdown y ensamblaje del paquete `{fecha_inicio_clases, fecha_fin_clases, horarios:[...]}`; recibe `Uint8List bytes`+`mimeType` para ser cross-platform); `application/escanear_horario_provider.dart` (`aiScheduleParserServiceProvider`, `guardarFechasSemestre()`, motor `generarHorariosDesdePaquete()` que materializa el patrón en ocurrencias semanales fechadas en `horarios_academicos` con `es_fijo=false`+`es_hito_inamovible=true`, idempotente vía `ics_uid` sintético); `presentation/widgets/escanear_horario_boton.dart` (flujo Clean UI: verifica fechas de semestre en el perfil → Rama A `BottomSheet` plano para pedirlas y guardarlas / Rama B fricción cero, `file_picker` con `withData:true`, `LinearProgressIndicator`/`CircularProgressIndicator`, SnackBar «Horario sincronizado con éxito»); integrado en `app/lib/features/perfil/presentation/perfil_screen.dart` (tarjeta "Mis asignaturas" → pestaña Académico). `PerfilAcademicoDb` ampliado con `fechaInicioClases`/`fechaFinClases` + getter `tieneFechasSemestre`.
- **`app/lib/features/academico/` (Vista Académica + repositorio de archivos)** — Pantalla `AcademicScreen` (`presentation/academic_screen.dart`, ruta `/academico` = pestaña inferior "Académico", Flat Design fondo #1A1A2E): header con título "Académico" + botón calendario → `/academico/planificar`, grid 2 columnas de `AsignaturaCard` (siglas destacadas, franja vertical de color semántico, métricas "X apuntes • Y archivos"). `AsignaturaDetalleScreen` (`presentation/asignatura_detalle_screen.dart`, ruta `/academico/asignatura/:id`, recibe `AsignaturaDb` vía `extra`): TabBar plano personalizado de 2 pestañas fusionadas: **Progreso** (Dashboard + Tareas unificados: rastreador de temario, timeline de la asignatura, simulador de calificaciones, directorio docente) y **Temario** (Apuntes + Archivos unificados: lista de notas + botón IA, lista de archivos con subida/eliminación). `MaterialEstudioDb` en `shared/models/db_models.dart` (wrapper SM-2 para materiales de estudio). Provider `materialesEstudioProvider` (`application/materiales_estudio_provider.dart` → `materialesEstudioRepositoryProvider`, `materialesAsignaturaProvider.family`). Widgets en `presentation/widgets/` (`asignatura_card.dart`, `archivo_tile.dart`). Dominio: `domain/archivo_asignatura_dto.dart` (`ArchivoAsignaturaDto` + enum `TipoArchivo` con icono/color), `domain/asignatura_visual.dart` (`colorAsignatura`/`siglasAsignatura`). Infraestructura: `infrastructure/archivos_asignatura_repository.dart` (`ArchivosAsignaturaRepository` + `ArchivoStorageException`: sube el binario a Cloudflare R2 vía el Worker proxy con `PUT` Dio + progreso y clave estricta `usuarios/{user_id}/asignaturas/{asignatura_id}/archivos/{ts}_{nombre}`, registra metadatos en `archivos_asignatura` con rollback best-effort). Providers: `application/archivos_asignatura_provider.dart` (`archivosRepositoryProvider`, `archivosAsignaturaProvider.family`), `application/apuntes_asignatura_provider.dart` (`apuntesPorAsignaturaProvider.family`), `application/conteos_asignatura_provider.dart` (`conteosAsignaturaProvider.family` → `({int apuntes, int archivos})`), `application/tareas_asignatura_provider.dart` (`tareasAsignaturaProvider.family` → `List<TimelineItem>` con bloques + entregas/exámenes de la asignatura desde hoy), `asignaturaPorIdProvider.family` en `asignaturas_provider.dart`. `shell_route.dart`: la pestaña Académico (índice 1) ya no salta directa al planificador, ahora muestra `AcademicScreen`.
- **`app/lib/features/academico/` (Flashcards + Simulacro + Tarjetas de Estudio)** — `flashcards_screen.dart` (pantalla de tarjetas digitales con clasificación dominadas/dudosas/falladas), `flashcards_results_screen.dart` (resultados con estadísticas), `simulacro_test_screen.dart` (test simulado cronometrado con puntuación), `resultados_simulacro_screen.dart` (resultados detallados del simulacro), `tarjetas_estudio_screen.dart` (visor de tarjetas de estudio), `cuestionario_formato_screen.dart` (selector de formato de preguntas). Providers: `flashcards_provider.dart` (datos de flashcards). Rutas: `/academico/flashcards/:materialId`, `/academico/flashcards/:materialId/revision`, `/academico/flashcards/:materialId/resultados`.
- **`app/lib/features/academico/` (Centro de Generación Multi-Fuente)** — `centro_generacion_screen.dart` (pantalla unificada de generación IA: resúmenes, mapas mentales, cuestionarios desde múltiples fuentes), `seleccion_fuentes_sheet.dart` (bottom sheet con checkboxes de apuntes/archivos), providers: `artefacto_efimero_provider.dart` (estado de generación efímera con TipoGeneracion/EstadoGeneracion), `generacion_background_provider.dart` (colas de generación en segundo plano), `generacion_global_provider.dart` (estado global de generaciones en curso), `multi_tema_provider.dart` (generación multi-tema).
- **`app/lib/shared/widgets/badge_generacion_widget.dart`** — Widget persistente en el bottom del shell que muestra el estado de tareas de generación IA en segundo plano (pulsa/animación al completar).
- **`app/lib/core/session_reset.dart`** — Utilidad para destruir y recrear el ProviderScope al hacer logout, asegurando que todos los providers se reinicien limpiamente.
- **`app/lib/features/admin/`** (Shadowban + GDPR): `admin_provider.dart` con `toggleShadowban()`, `getLockdownState()`, `toggleLockdown()`, `anonymizeUser()`, `exportUserData()`. `admin_repository.dart` extendido con métodos shadowban/lockdown/anonymize/export. `AdminHubScreen` con toggle de Lockdown y botones GDPR. `AdminUsuarioDetalle` con switch de shadowban y acciones GDPR.
- **`app/lib/features/academico/` (Asistente de estudio con IA: resúmenes + mapas mentales)** — Resume apuntes/archivos y genera mapas mentales estilo NotebookLM. Dominio: `domain/fuente_estudio.dart` (sealed `FuenteEstudio` → `FuenteTexto` para apuntes / `FuenteArchivo` para PDF o imagen), `domain/mapa_mental.dart` (`MapaMental`/`NodoMental` + `fromJson` tolerante, profundidad máx. 3). Infraestructura: `infrastructure/estudio_ia_service.dart` (`EstudioIaService` + `EstudioIaException`: Gemini (`EnvConfig.geminiModel`); `resumir()` devuelve Markdown y `mapaMental()` devuelve JSON; multimodal vía `inline_data` base64 para archivos descargados con Dio). Provider: `application/estudio_ia_provider.dart` (`estudioIaServiceProvider`). Pantallas (Clean UI): `presentation/resumen_ia_screen.dart` (`ResumenIaScreen` con `MarkdownBody` + botón "Ver mapa mental"), `presentation/mapa_mental_screen.dart` (`MapaMentalScreen` + árbol interactivo: layout izquierda→derecha con `CustomPaint` de aristas bezier, `InteractiveViewer` pan/zoom, nodos expandibles/colapsables coloreados por rama). Entrada compartida: `presentation/widgets/asistente_ia_sheet.dart` (`mostrarAsistenteIa()` → bottom sheet Resumen/Mapa). Integrado como botón ✨ "Asistente IA" en `ApunteVisorScreen` (FuenteTexto) y en `ArchivoVisorScreen` (FuenteArchivo, solo PDF/imagen). FIX: `presentation/archivo_visor_screen.dart` — `_pdfController` pasó de `late final` a nullable para corregir el `LateInitializationError` temporal al previsualizar PDFs (lo leía `build` antes de la asignación asíncrona). PERSISTENCIA (volver a ver los documentos sin regenerar): `infrastructure/documento_ia_repository.dart` (`DocumentoIaRepository` + DTO `DocumentoIa` + enum `TipoDocumentoIa` con 4 valores: resumen/mapaMental/guiaDocente/practica: lee/guarda en la tabla `documentos_ia` con UPSERT por `(usuario_id, fuente_tipo, fuente_id, tipo)`, más `existencias()` → `({bool resumen, bool mapa})`) y `application/documento_ia_provider.dart` (`documentoIaRepositoryProvider`, `docsGuardadosProvider.family` con clave record `(fuenteTipo, fuenteId)` para los badges). `FuenteEstudio` ahora lleva `fuenteId`/`fuenteTipo`/`asignaturaId`; `MapaMental.toJson()` serializa el mapa. `ResumenIaScreen`/`MapaMentalScreen` hacen CARGAR-O-GENERAR: si existe el documento guardado lo muestran al instante (`ResumenIaScreen` con chip "Resumen guardado · fecha"), si no lo generan y autoguardan, y "Regenerar" sobrescribe. `asistente_ia_sheet.dart` (`ConsumerWidget`) marca con badge "Guardado" las acciones ya persistidas. FIX UI: en `AsignaturaDetalleScreen` el `_FlatTabBar` se dirige por `controller.animation` (no `controller.index`), eliminando el lag del indicador de foco al deslizar entre pestañas. `_ApunteRow`/`ArchivoTile` (`ConsumerWidget`) muestran iconos intuitivos (📄 resumen / 🕸️ mapa mental) en cada apunte y archivo que tenga documentos IA guardados; al pulsarlos navegan directamente al resumen o mapa mental. GENERACIÓN MULTI-FUENTE: `application/artefacto_efimero_provider.dart` (`TipoGeneracion` enum — resumen/mapaMental/cuestionario—, `EstadoGeneracion` enum, `ArtefactoEfimeroState`, `ArtefactoEfimeroNotifier` con `generarResumen()`/`generarMapaMental()`/`generarCuestionario()` para múltiples fuentes vía `EstudioIaService.resumirMulti()`/`mapaMentalMulti()`/`generarPracticaMulti()`, `PreguntaGenerada`, provider `artefactoEfimeroProvider` autoDispose). Widget `presentation/widgets/seleccion_fuentes_sheet.dart` (`mostrarSeleccionFuentes()` → bottom sheet Flat Design con checkboxes de apuntes y archivos, crea `List<FuenteEstudio>` + `ArtefactoEfimeroNotifier`, navega a `CentroGeneracionScreen` con `ProviderScope` override). Pantalla unificada `presentation/centro_generacion_screen.dart` (`CentroGeneracionScreen` ConsumerStatefulWidget: recibe `tipo`/`asignaturaId`/`color`, auto-dispara generación en initState, 3 estados — cargando (skeleton CircularProgressIndicator), error (icono + mensaje + Reintentar), completado (MarkdownBody para resumen / `MapaMentalVista` árbol interactivo para mapa mental — nodos expandibles con aristas Bézier, InteractiveViewer pan/zoom), BottomAppBar Flat Design con Regenerar (IconButton), Descartar (IconButton), Guardar como apunte (FilledButton: llama `crearApunte()`, invalida `apuntesPorAsignaturaProvider`, SnackBar, pop). Helper `_mapaToMarkdown()` para serializar el mapa mental como apunte Markdown.
- **`app/lib/features/bienestar/presentation/nueva_rutina_screen.dart`** — Creación de rutinas con IA (3 pasos) + edición de nombres de semanas y días vía dialog con TextField, maps `_nombresSemanas`/`_nombresDias`, métodos `_editarNombreSemana()`/`_editarNombreDia()`. `_recomendarRutina()` aplica los nombres sugeridos por la IA a `_nombresSemanas`/`_nombresDias` (se muestran en creación y se persisten); tarjetas de ejercicios reordenables con botones subir/bajar (sin `ReorderableListView` para evitar el atasco del scroll vertical); timeouts de generación ampliados (75s pantalla) y errores reales en el SnackBar vía `_resumenError()`/`_acortarMotivo()`.
- **`app/lib/core/sync/`** — SyncHub (Fase 2): `DominioEvento` enum (13 eventos: planGuardado, bloqueEstudioCompletado, bloqueEstudioDesmarcado, sesionCompletada, sesionDesmarcada, checkInRealizado, entregaCompletada, retoCompletado, pomodoroCompletado, xpOtorgado, practicaCompletada, shadowbanToggled, lockdownToggled), `EventoPayload` DTO con `horarioId`, `SyncHub` con mapa de invalidaciones simétricas por evento (ida y vuelta invalidan mismos providers neurofisiológicos + retos + timeline + dashboard), `syncHubProvider` (Riverpod Provider).
- **`app/lib/shared/widgets/exercise_metrics.dart`** — `SemanticMicroChip`, `ExerciseMetricsRow`, `SemantiCalorieChip` (chip naranja/gris con kcal), `buildCalorieChip()` (helper con MET + dualidad planificación/real), enum `ExerciseMetricCategoria` con `desdeModalidad`/`desdeFinalidad`. Aplicado en `nueva_rutina_screen`, `rutina_detalle_screen`, `sesion_en_vivo_screen`, `rutinas_comunidad_screen`.
- **Localización (i18n):** `flutter_localizations` en `pubspec.yaml`. `MaterialApp.router` con `localizationsDelegates` (GlobalMaterialLocalizations/GlobalWidgetsLocalizations/GlobalCupertinoLocalizations) y `supportedLocales` (`es_ES`, `es`, `en`). `DateFormat` con locale `'es'` en `canvas_screen.dart`. Todos los `showDatePicker` en español.
- **`supabase/migrations/`** — 62 archivos de migración. 0022 materiales_estudio (tabla wrapper SM-2 + amplía CHECKs en documentos_ia para 'practica' y horarios_academicos para 'repaso'). 0023 bancos_preguntas (tablas bancos_preguntas, preguntas, intentos_pregunta para tests generados por IA). 0024 fix_rls_preguntas (extiende política RLS de SELECT a FOR ALL en preguntas + admin). 0002-0004 renombrados para evitar colisión; 0004 con DROP VIEW IF EXISTS; 0005 corrige fechas; 0006 admin_rol; 0007 nivel_actividad; 0008 asignaturas_usuario_semestre; 0009 admin_panel_v2; 0010 admin_delete_user; 0011 calendar_grid (es_fijo + dia_semana); 0012 xp_planificacion (xp_planificacion_otorgado en planes_estudio); 0013 bloque_xp_tracking (xp_bloque_otorgado + xp_entrega_otorgado); 0014 retos_bloques_bridge (reto_id + hito_id FK); 0015 dia_rutina_fk (dia_rutina_id + semana_rutina_id FK); 0016 rutina_fk_fix (rutina_id → FK real); 0017 trigger_hito_progreso (trigger progreso automático de hitos); 0018 unique_dias_rutina (UNIQUE + CHECK en dias_rutina); 0019 fix_duplicate_fk_rutina (elimina FK duplicada en horarios_academicos); 0022 lienzo_continuo (amplía CHECK tipo_actividad + FK a entregas_examenes + nuevas columnas en entregas_examenes); 0023 lienzo_continuo_v2 (columna es_hito_inamovible BOOLEAN + índice idx_horarios_fecha_rango en horarios_academicos); 0024 ics_sync (soporte importación/exportación ICS); 0025 ics_upsert_fix (corrección UPSERT en sincronización ICS); 0001 dias_disponibles_array; 0002 fix_nivel_umbral (umbral de subida de nivel = 100 × nivel, consistente con la UI); 0003 xp_overflow_multinivel (subida de nivel inmediata + arrastre del sobrante multinivel en `otorgar_xp`, evita mostrar XP por encima del objetivo); 20260622000004 visibilidad_rls (activa RLS en `rutinas`/`retos`/`horarios_academicos` con políticas dueño + admin + lectura de pares según `nivel_privacidad`/`amistades`; funciones SECURITY DEFINER `es_admin`/`nivel_privacidad_de`/`son_amigos`; trigger `trg_invalidar_contenido_privado` que privatiza rutinas/retos al pasar el perfil a 'privado'); 20260622000005 carga_xp_estudio_otorgado (añade la columna `xp_estudio_otorgado` que faltaba en remoto en `carga_academica_semanal`, corrige divergencia de esquema y el error 42703 al sincronizar la carga académica); 20260622000006 retos_sinergia_v2 (Fase 1 del refactor de Retos: añade `asignatura_id`, `dificultad` baja/media/alta, `entidad_vinculada_id` y `entidad_vinculada_tipo` examen/entrega a `retos` y `hitos_de_reto`, con CHECKs e índices para el deep-linking con la agenda); 20260622000007 retos_xp_tracking (Fase 5 del refactor de Retos: columna `xp_otorgado` en `retos`/`hitos_de_reto` para registrar el XP exacto otorgado por el estado completado actual —anti-farmeo— y RPC `restar_xp` que resta XP con bajada de nivel segura, inverso de `otorgar_xp`); 20260622000008 retos_deep_linking (sincronización bidireccional reto/tarea ⇄ examen/entrega: helper SQL `xp_por_dificultad` + triggers `trg_sync_entrega_a_reto` (con ajuste de XP coherente vía guarda `xp_otorgado`), `trg_sync_reto_a_entrega` y `trg_sync_hito_a_entrega`, todos SECURITY DEFINER y con anti-bucle `WHEN OLD IS DISTINCT FROM NEW` + guarda `IS DISTINCT`); 20260622000009 social_realtime (añade `actividades_sociales`, `interacciones_sociales` y `comentarios_feed` a la publicación `supabase_realtime` con `REPLICA IDENTITY FULL` para que el muro social se actualice en tiempo real; reasegura la lectura pública del feed `actividades_sociales`); 20260622000010 perfil_fechas_semestre (añade `fecha_inicio_clases` y `fecha_fin_clases` DATE a `perfil_academico_usuario` para que el flujo de escaneo de horarios con IA adjunte las fechas absolutas al patrón semanal extraído del documento); 20260622000011 social_publicaciones_v2 (añade columnas a `actividades_sociales` para tipos de publicación enriquecidos); 20260622000012 procedencia_clones (columna `procedencia` en `rutinas` para trazabilidad de clones); 20260622000013 archivos_asignatura (tabla `archivos_asignatura` con metadatos de archivos adjuntos a una asignatura —`cloudflare_object_key`, `url_publica_o_firmada`, `tamano_bytes`, `tipo_mime`—, FK a `asignaturas` ON DELETE CASCADE, RLS dueño + admin e índices; el binario físico vive en Cloudflare R2); 20260622000014 apuntes_actualizado_en (añade columna `actualizado_en` a `apuntes` que faltaba en migración original, corrigiendo error al ordenar/actualizar apuntes); 20260622000015 apuntes_visibilidad_fix (reemplaza el CHECK heredado en español de `apuntes.visibilidad` —privado/publico/amigos, creado por la migración antigua 202605050012 y no sustituido por el `CREATE TABLE IF NOT EXISTS` de 0004— por los valores canónicos de la app `private`/`public`/`solo_amigos`, normaliza los datos existentes y restablece el DEFAULT a `private`; corrige el error 23514 `apuntes_visibilidad_check` al crear apuntes); 20260622000016 documentos_ia (tabla `documentos_ia` que persiste los materiales generados por el asistente de IA —resúmenes en Markdown y mapas mentales en JSON— con UPSERT por `(usuario_id, fuente_tipo, fuente_id, tipo)` para sobrescribir al regenerar, RLS dueño + índices; permite volver a ver los documentos sin regenerarlos); 20260622000017 documentos_ia_ampliar_check (amplía los CHECKs de `fuente_tipo` y `tipo` en `documentos_ia` para aceptar `guia_docente`, necesario para el Dashboard Inteligente de Asignatura); 20260625000018 perfil_academico_rls (habilita RLS en `perfil_academico_usuario` —requerido por el nuevo Academic Control Center que guarda fechas de semestre— con políticas owner + admin); 20260625000019 usuario_insignias_update_asignaturas_rls (añade UPDATE policy en `usuario_insignias` para `marcarNotificada()` + habilita RLS en `asignaturas` con políticas owner + admin); 20260626000020 valor_met_ejercicios (columna `valor_met` DOUBLE PRECISION DEFAULT 6.0 en `ejercicios` + recrea `v_ejercicios_completos` para exponer el coeficiente MET del Compendio de Adultos 2024 al CalorieCalculatorService); 20260626000021 duracion_real (RENAME `duracion_segundos` → `duracion_objetivo_segundos` + ADD `duracion_real_segundos` en `seleccion_de_ejercicios` para la dualidad planificación vs ejecución real). 20260628000025 test_sessions (tabla test_sessions para sesiones de práctica persistentes con subconjunto del banco de preguntas). 20260629000026 horarios_metadata (columnas is_private BOOLEAN + tipo_clase VARCHAR en horarios_academicos). 20260701000027 cognitive_study_cost (★ Fórmulas 1 y 2: extiende horarios_academicos con met_value/calorias_quemadas/carga_cognitiva_generada, crea estado_cognitivo_usuario con trigger trg_inicializar_estado_cognitivo). 20260701000028 physical_workload_and_srs (★ Fórmulas 3 y 4: crea registros_carga_fisica con carga_diaria GENERATED, estado_regulacion_cruzada con acwr_actual GENERATED, registros_repaso_srs para auditoría SRS, trigger bidireccional trg_insertar_carga_fisica, RPC recalcular_regulacion_cruzada). 20260701000029 shadowban_lockdown (añade columna is_shadowbanned en usuarios, crea tabla configuracion_global con lockdown_activo, RLS filtrado de shadowbanned, trigger trg_lockdown_shadowban_nuevo). 20260701000030 gdpr_anonimizacion (RPC anonymize_user en 3 fases, RPC exportar_datos_usuario con 22+ secciones, delete_user mejorado). 20260701000031 fix_gdpr_rpcs (corrige columnas mal referenciadas en anonymize_user y exportar_datos_usuario). 20260701000032 fix_export_rpc (corrige ORDER BY en exportar_datos_usuario). 20260701000033 fix_insignias_column (corrige columna obtenida_en vs creado_en en exportar_datos_usuario). 20260831000034 hitos_adjuntos (añade `apunte_id` y `archivo_id` a `hitos_de_reto` con FK ON DELETE SET NULL a `apuntes`/`archivos_asignatura` + CHECK `ck_hitos_adjunto_unico` para adjuntar material académico a las tareas de reto). 20260831000035 horarios_nullable_y_bloques (hace NULLABLE `horarios_academicos.asignatura_id` para que los bloques de deporte/retos sin asignatura se persistan correctamente, y amplía los CHECKs `ck_retos_entidad_tipo`/`ck_hitos_entidad_tipo` para aceptar 'bloque' como vinculación a bloques de estudio del calendario). 20260831000036 fix_legacy_checks (corrige divergencias de esquema heredadas de la BD remota pre-consolidación: normaliza `planes_estudio.visibilidad` de 'publico'/'privado'/'solo_amigos' a 'private'/'friends'/'public' —el INSERT del plan fallaba SIEMPRE al guardar el lienzo— y elimina el CHECK antiguo `horarios_academicos_tipo_actividad_check` que rechazaba descanso/comida/sueno/examen/entrega/repaso).
- **`docs/`** — 25-file documentation structure: 00-plan-maestro → 23-plan-backoffice-hibrido, kept in sync with code. Updated for Sprint Fórmulas Neurofisiológicas (v8.0), GDPR/Shadowban y Flujo Unificado de Retos (v8.2).

## Database notes
- All tables have Row Level Security enabled; `ejercicios` + catalog tables are public-read
- User-owned tables are owner-only write; some allow peer visibility
- `auth.users` ↔ `public.usuarios` sync via trigger (migration 0004)
- If adding/modifying tables: create a **new** migration file, never edit old ones

## Git safety (Gatekeeper)
Per `.agents/rules/actualizacion-git.md`:
- **Never** commit/push automatically after writing code
- Pause and ask user to test manually; wait for explicit "OK" / "Aprobado" / "Funciona"
- Commit messages in **Spanish**, Conventional Commits format: `feat: [Módulo] Descripción`
- Push only to `master`

## Equipo de agentes (.opencode/agents/)

| Agente | Archivo | Rol |
|--------|---------|-----|
| `corrector` | `corrector.md` | QA — ejecuta `dart format .` + `flutter analyze`, reporta y corrige issues de lint/formato |
| `desarrollador` | `desarrollador.md` | Dev Flutter/Dart — implementa features, corrige bugs, escribe código de producción |
| `diseñador` | `diseñador.md` | Arquitecto — diseña estructura de carpetas, esquemas BD, contratos de API, diagramas Mermaid |
| `documentacion` | `documentacion.md` | Tech Writer — sincroniza `docs/` (25 archivos) con el código fuente, verifica discrepancias |
| `product-manager` | `product-manager.md` | PM — investiga mercado, define MVP y requisitos, escribe `docs/02-requirements.md` |

## Skills disponibles (.agents/skills/)

| Skill | Carpeta | Propósito |
|-------|---------|-----------|
| `equipo-jloen` | `equipo-jloen/SKILL.md` | Protocolo de orquestación: ciclo de vida de features, gatekeeping, doc-sync obligatorio, flujo Git |
| `frontend-design` | `frontend-design/SKILL.md` | Diseño de interfaces frontend con alta calidad estética |

## Regla de sincronización de AGENTS.md

Todo agente o skill que modifique el código del proyecto debe:
1. **Leer `AGENTS.md` al iniciar** para tener contexto completo del proyecto.
2. **Actualizar `AGENTS.md` al finalizar** si sus cambios afectan: stack tecnológico, arquitectura, estructura de carpetas, rutas, dependencias, comandos, estructura de `docs/`, o convenciones de código.

## Regla de sincronización docs ↔ código

Todo cambio en el código que afecte APIs, rutas, modelos, tablas, dependencias o arquitectura **debe** reflejarse en `docs/`. El agente `documentacion` es responsable de esta sincronización.

| Cambio en código | Docs a actualizar |
|-----------------|-------------------|
| Nueva feature completa | `14-changelog.md` + doc del módulo afectado |
| Nuevo archivo/carpeta | `AGENTS.md` (§Architecture) + `03-architecture.md` |
| Nueva migración | `AGENTS.md` + `04-data-model.md` + `07-backend.md` |
| Nueva ruta | `06-frontend.md` (§2) |
| Cambio de nombre de archivo/clase | `AGENTS.md` + docs que lo referencien |
| Feature eliminada | Eliminar de TODOS los docs que la mencionen |

### Verificación de consistencia docs ↔ código

```bash
# Verificar que no hay referencias a carpetas de features inexistentes
grep -rn "planificador/" docs/  # Debe devolver 0 resultados

# Verificar que los nombres de providers en docs existen en código
grep -rn "Provider\|provider" docs/ | grep -v "AGENTS.md"

# Verificar conteo de migraciones
ls supabase/migrations/*.sql | wc -l  # Comparar con AGENTS.md y docs/
```

El agente `documentacion` DEBE ejecutar esta verificación al final de cada sprint.
